import styled from "styled-components";

const StyledMessage = styled.span`
  width: 120px;
  margin-top: 10px;
  margin-left: 5px;
  color: red;
  font-weight: bold;
  font-size: 10px;
`;

export default StyledMessage;
