import styled from "styled-components";

export const StyledTotalUnreadMessages = styled.div`
  display: flex;
  align-items: center;
  gap: 5px;
  color: var(--aqua);
  font-size: 12px;
  font-weight: 500;
`;

export const StyledCounter = styled.span`
  font-size: 20px;
  font-weight: 600;
  color: inherit;
`;
